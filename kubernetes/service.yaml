apiVersion: v1
kind: Service
metadata:
  name: csp-email
  namespace: cspapi
spec:
  selector:
    app: csp-email
  type: ClusterIP
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080